# AGENTS

Archived full notes: `docs/archive/AGENTS.md.full`.

## Run

```bash
bash .cursor/install.sh   # optional, manual
npm run dev               # port 5000
```

Do not reattach the old saved Cloud Agents environment.
